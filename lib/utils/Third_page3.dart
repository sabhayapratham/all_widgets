import 'package:flutter/material.dart';

class Third_page3 extends StatelessWidget {
  const Third_page3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("third page"),
    );
  }
}
