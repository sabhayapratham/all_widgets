import 'package:flutter/material.dart';

class Second_page2 extends StatelessWidget {
  const Second_page2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text("second page"),
    );
  }
}
